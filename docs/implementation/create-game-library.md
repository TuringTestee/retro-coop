Create Game becomes a separate page backed by one browser library of exact NES files. The room browser stays focused on joining. The waiting host can open or close an empty Guest place, and all pages keep content in stable regions without overlays. New behavior in this amendment waits for review, user approval of the reviewed version, and merged governing documents.

Audience: Agent

# Create Game and local ROM library delivery plan

## Sources and review snapshot

The [human direction](../design/create-game-library-direction.md), [current ASCII design](../design/create-game-library-wireframe-v4.md), [v4 critique](../design/create-game-library-critique-v4.md), [journeys](../design/create-game-library-journeys.md), [scenarios](../design/create-game-library-scenarios.md), and [reference study](../design/create-game-library-references.md) define the proposed experience. The separate Create Game/library scope was approved in PR #101 and merged as `5a6bf2b`; the later Warcraft III slot and fixed-layout direction is a behavior/constraint amendment requiring this reviewed approval. The approved [room transfer](room-rom-transfer.md) still governs private upload, guest acquisition, and host Start. At this snapshot, `main` is `5a6bf2b`; `RoomPanel.tsx` still renders the inline host bar and automatically calls `host()` after `LocalPlayer.load`, while `saves.ts` stores only downloaded verified ROM bytes by SHA-256. No implementation of the Create Game or slot/layout amendment has begun.

**Source precedence when this amendment is approved and merged:** This document and its linked human direction replace the inline host entry, automatic create after file selection, browser library/preview behavior, waiting Guest-place admission controls, and overlay navigation in `lobby-refactor.md`, `browser-nes-platform.md`, older lobby journey/scenario/wireframe files, and gate #66's J5 text. Their first-host 0/2 offer, host Start, guest Prepare, privacy and recovery rules remain except that an empty hosted P2 may be closed. The room-transfer documents continue to own exact-byte upload and guest download. Gate #66 plus epic #2 must be updated to the approved revision before changed-scope dispatch. README and running UI retain the old entry path until CG2 integrates; after integration they must describe the new one, with no alternate host control left behind.

## Architecture and reuse

1. **Separate page state:** Give the app an explicit Public rooms / Create Game / waiting room / play view state. Route the Public rooms header's single **Create game** action to the setup page; browser Back and **Back to rooms** return to the same directory state. Invitations still open their own room preview. Keep the existing Standard/Relay-only selector beside public Join and invitation Join, and move its host choice beside **Create room**. The selected policy must reach the same existing `RoomClient`/peer path; Relay-only failure remains visible and never falls back silently. Remove the inline host bar and any duplicate setup file picker from the player panel. Keep the existing directory, room client, waiting room, and gameplay owners.
2. **One browser ROM library:** Extend the existing `saves.ts` IndexedDB `roms` owner to hold a bounded local display label, source, last-used time, and optional small preview beside verified bytes, all keyed by exact SHA-256. Migrate existing downloaded records without losing bytes. Bundled games are derived from the verified catalog manifest and appear once without duplicating their bytes in IndexedDB. A local import and guest download of the same hash update one entry. Public room metadata still uses the neutral generated room name; local filename/label/preview never leaves the browser. Local data deletion and clear generation guards remain authoritative, including cross-tab races.
3. **Safe selection and host load:** Add file validates through the existing cartridge/fingerprint path, computes exact SHA-256, then stores a verified copy when possible. Selecting a saved row rehashes bytes, checks size, reconstructs a `File`, and passes it through the same `LocalPlayer.load` path as a new import. The Create page owns one selected candidate `{sha256, file, fingerprint, generation}`; **Create room** stays disabled until that candidate and the locally loaded player fingerprint match. A newer selection, Cancel, room change, or component exit invalidates the generation so an older load completion cannot host the wrong file. A denied/quota write leaves the validated file in tab memory with an honest notice. A corrupt/evicted record or failed local load deselects the row and offers **Add NES file** without creating a room. Reuse `acquireVerifiedRom` where it fits; keep its per-record and whole-store deletion generations rather than writing a second ROM cache.
4. **Deliberate creation:** Decouple `LocalPlayer.load` from automatic `RoomClient.host()`. File or library selection prepares local bytes and preview only. **Create room** is the sole trigger for the existing hidden create → private upload → server verify → publish transaction for custom files. Pass the selected candidate's `File` and fingerprint together to `RoomClient.host()` instead of relying on its mutable `selectedUploadFile` left by a previous load. At upload completion, the candidate and loaded player must still match the room fingerprint; a changed candidate cancels the intent. For bundled games, extend the proven atomic 0/2 `claimCode` command to accept the chosen visibility at claim time. Public retains the current claim behavior; Unlisted converts the offer directly into an invite-only room without ever publishing an intermediate public human room. The service replenishes the public 0/2 offer after either claim. No duplicate bundled upload or second included-room path is added. If no healthy offer is available, show that state beside Create and return to the room browser for another game. Cancel or failure returns to selected setup without a public ghost row; switching files cannot replace an active room implicitly.
5. **Real preview:** Capture a small frame only after a game has actually rendered nonblank content, associate it with the exact hash, and update last-used on actual play/hosting. Restrict encoded dimensions/bytes and reject malformed stored preview data. When there is no usable frame, render text “No preview yet”; do not show a blank image or invent cover art. Preview is display only, never another way to select/host.
6. **Focused UI:** Build Create Game as its own component/page with a scrollable game list left and selected game's real preview right at wide sizes, stacked in document flow at narrow sizes. Include one Add NES file, selected-game summary, Public/Unlisted choice, and Create room. Keep Local data deletion in Settings. Make status, focus, disabled reason, keyboard selection, and narrow layout match v4. Remove old inline host copy and stale instructions from the public entry point and README.
7. **Server-owned Guest place:** Add an explicit `open|closed` admission field to hosted waiting rooms and their room/directory/invitation views, distinct from occupancy and visibility. Default Open; bundled 0/2 offers remain claimable and have no host slot control. Add a host-only command to Close or Open P2 with a room ID and a version/current-state guard. Under the coordinator's synchronous room mutation, Close succeeds only when confirmed, waiting, and empty, after checking reservations and reconnect rights; it atomically prevents new Join, Join by code/invite, and reconnect admission. Occupied Close returns a specific state error and leaves P2 Open. After Remove guest has revoked that membership, host may Close. Open restores admission before Start. Start removes control eligibility and the current no-late-join rule remains. Propagate changes through directory/preview and guest views so stale clients see the actual status. Closed public rooms stay listed with no Join, and unlisted invitations show Closed. Do not change two-player capacity or leak filenames.
8. **Stable pages, no overlays:** Model Public rooms, Create Game, waiting, invitation, playing, Settings, Local data, Saves, Rewind and Game help as page states with one Back path and focus return. Replace `showModal()` dialogs in `Settings.tsx`, `LocalData.tsx`, `Saves.tsx`, `Rewind.tsx`, and `main.tsx`; replace the fixed `.room-panel` drawer/invitation, `.release-notice`, `.selection-status`, and `window.confirm` calls in `RoomPanel.tsx`. Put confirmations and errors in the relevant page's action area, preserving cancel/failure state. Preserve the existing supported Save import/export/current-backup/retry and Local data save/battery/preferences export/delete actions, showing each only in its usable state; hide Load save and Rewind in shared play because the current player rejects shared restore. The room panel is a grid region beside the canvas at wide sizes and below it at narrow sizes; no floating panel may cover gameplay. Avoid duplicate controls in visually hidden but focusable DOM. Keep the existing full-screen canvas control only if it is explicit and does not put app controls above content. Use stable labels and normal page scroll; reduced-motion users see the same path.

## Delivery dependencies and acceptance

Record this amendment under epic [#2](https://github.com/TuringTestee/retro-coop/issues/2). The existing open [journey gate #66](https://github.com/TuringTestee/retro-coop/issues/66) must be updated because its J5 still requires immediate creation and a guest-provided matching file. It remains the one integrated gate for this arrival-to-play scope. After approval, reconcile the four bounded deliveries and their dependency/Project entries; CG1/CG2 are carried forward from PR #101, while CG3/CG4 require this revision:

| Priority | Delivery | Depends on | Acceptance |
| --- | --- | --- | --- |
| P0 CG1 | Unified browser ROM library and real recent preview | Approved merged plan, existing ROM store and catalog | Local imports and guest downloads of identical bytes produce one hash row; older downloaded records remain readable; saved selection is reverified and locally loaded; eviction, corrupt bytes, load failure, quota denial, cross-tab Clear and deletion are honest; no filename/image leaves browser metadata. Preview shows a real frame or text fallback, never blank. |
| P0 CG2 | Separate Create Game page and deliberate room creation | CG1 merged and integrated; existing private upload and included-room claim | Home has one Create game action and no host form; setup has one selection path and one Create room action; Public/Unlisted, Standard/Relay-only, Back, Cancel, failed upload and retry preserve correct state. Included-game Unlisted claim has no transient public human-room listing and replenishes the 0/2 offer. Two independent browsers can host a saved custom ROM, guest auto-download/Prepare, and Start shared play. Included 0/2 offers, unlisted invites and local play regressions pass. |
| P0 CG3 | Host Guest-place Open/Closed admission | CG2 merged; coordinator contracts and room store | Host can close/reopen empty P2; Close is rejected if a guest/reservation/reconnect exists; occupied guest must be removed first; closed public/invite views show status and no Join; stale Join/code/invite/reconnect cannot bypass closed admission; no slot control after Start; two-browser race and keyboard evidence. |
| P0 CG4 | Stable no-overlay pages and lean content | CG2 merged; may develop alongside CG3 after approved plan | Settings, Local data, Saves, Rewind, Game help, invite, room state and status use fixed page regions and one Back/exit path; no dialogs, fixed floating drawers/toasts or browser confirms cover content; inline confirmation works with keyboard; 200% zoom/narrow and wide screenshots show canvas and room state without overlap; delete unsupported or duplicate controls. |
| P0 gate #66 | Integrated changed journeys J1–J10 | CG1–CG4 merged; existing room-transfer integration | Current public-entrypoint wide/narrow screenshots and keyboard walk show all ten journeys and main recovery paths; adversarial UI review using the shared six principles is retested; two browser roles reach 200+ matching frames on a custom game, included-game claim remains correct, slot Close/Join race is proven, and secondary pages do not overlay or obscure play. |

## Verification and rollout

- Focused tests cover SHA-keyed deduplication, metadata migration, verified selection and local load, stale candidate/changed file races, concurrent deletion, create/cancel/upload races, atomic Public/Unlisted included-offer claim, Relay-only privacy through Create and Join, and absence of public filename/preview leakage. Add coordinator contract tests for Close/Open authorization and idempotence, occupied/reconnect and concurrent Join/Close outcomes, directory/invite publication, and Start eligibility. Avoid tests that merely copy component rendering.
- Run `timeout 60s sh scripts/preflight.sh`; keep required PR CI within the current five-minute [verification strategy](browser-nes-platform.md#verification-strategy). Use a redistributable diagnostic ROM in CI and the user-supplied Super Tilt Bro file only for local manual proof, without committing or publishing its bytes.
- Test from `sh scripts/demo.sh` in two independent browser sessions. Inspect the recent preview image visually, including fallback; attach current-head screenshots of Public rooms, Create Game with saved/downloaded entries, empty/error state, Open/Closed/occupied waiting room, and play with room region. Walk keyboard, 200% zoom and narrow viewport through Settings, Local data, Saves, Rewind and Help, checking no content overlap. Verify a newly imported game appears after reload and a guest download is reusable for hosting. Run a stale Join versus Close race and confirm one coherent server-owned outcome.
- Use author draft review, fresh detached independent review, passing checks and merge policy B. Update README and conflicting historical gate/epic wording at integration; a new page is not proof of changed journey until those paths and the public entrypoint agree.

## Approval gate

PR #101 already approved and merged the separate Create Game and browser-library direction. The subsequent Guest-place controls and no-overlay layout change behavior and a major constraint. Obtain actual user approval of this exact independently reviewed planning PR head, then merge it before CG3/CG4 implementation. CG1/CG2 may use the earlier approval for unchanged scope, but final integration must follow this revision.
