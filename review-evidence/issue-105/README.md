Create Game now selects a verified local NES file before publishing a room. These screenshots and results show the separate setup page, a real recent frame, and a custom game shared by two browsers.

# CG2 browser evidence

- Run `sh scripts/foundation/prepare.sh`, then `RETRO_COOP_SKIP_INSTALL=1 RETRO_COOP_SKIP_PREPARE=1 RETRO_COOP_CLIENT_PORT=8895 RETRO_COOP_COORDINATOR_PORT=8897 sh scripts/demo.sh` from this branch. Ports 8765/8787 were occupied by another checkout, so this proof used 8895/8897; the full fixed-port entrypoint wrapper remained unavailable locally. Its `browser_check` function passed against 8895/8897.
- [Create Game](create-game.png) at 1280×1050: selected original diagnostic fixture, saved library row, included rows, Public/Standard choices, one Create room button, and Super Tilt Bro's actual 128×120 decoded recent frame. To produce the frame, select the supplied included build, choose Play locally, Resume, wait past 90 frames, then return to Create Game. The image remained decoded at 128×120 after reload; with no stored image the page displayed `No preview yet.`. The original-patterned fixture also passed `scripts/rooms/preview_browser.py` and its malformed-image fallback.
- [Narrow Create Game](create-game-narrow.png) at 390×700 had no horizontal overflow; keyboard Enter on Create game opened the page, and Back returned to Public rooms. [Host waiting room](host-waiting.png) appeared only after Create room.
- `scripts/rooms/two_agent_game.py --role run` with the generated diagnostic file, a private `COORDINATOR_ROM_DIR`, and separate Chromium processes passed [Public](two-browser-public.json) and [Unlisted](two-browser-unlisted.json). Each guest received no ROM path, downloaded the exact host bytes, prepared, and played more than 200 frames with matching paused state hashes and controller RAM `[128,64]`.
- An independent host/guest Chromium run selected the supplied Super Tilt Bro build, claimed an Unlisted 0/2 offer, joined by invitation, prepared, and reached 200 frames in both browsers with `Playing together` and no page errors. [Host gameplay](super-tilt-playing.png). A separate observer saw one new 0/2 Super Tilt Bro offer and no public human-room row during this Unlisted claim.
- `scripts/rooms/integrated_transfer_browser.py` passed failed-download retry, cache reuse, cross-tab Clear, quota-denied memory play, expired reservation and private blob deletion. `scripts/rooms/host_upload_browser.py` passed hidden upload cancellation, failure, expiry and retry. The browser entrypoint check passed included claim, invitation join, custom Public/Unlisted, shared play, failure/retry and invalid-file recovery. A routing regression found during author review was repaired: ordinary invitation hash changes no longer cancel lookup; Back from `/create` still cancels pending creation.

The two-browser fixture was generated locally and is not committed. No new user ROM bytes are included in this evidence or this change; the existing Super Tilt Bro catalog asset was already tracked before CG2.
